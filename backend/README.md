# Face Gallery API

FastAPI backend for offline face detection, clustering, and photo indexing.

## Dev

```powershell
.\scripts\dev.ps1
```

Or: `uv run uvicorn face_gallery.main:app --reload --host 127.0.0.1 --port 28765`
